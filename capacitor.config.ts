import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.pragma',
  appName: 'app-pragma-todo-ionic',
  webDir: 'www'
};

export default config;
